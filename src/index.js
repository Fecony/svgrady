class SVGRady {
  constructor ({ startAngle, endAngle, activeColor = 'black', color = 'gray' }) {
    this.startAngle = startAngle
    this.endAngle = endAngle
    this.activeColor = activeColor
    this.color = color
  }
}

export default SVGRady
